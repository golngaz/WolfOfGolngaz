{
  "name": "wog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir dist || rm -r dist && tsc",
    "start": "npm run build && node dist/src/main.js",
    "startWindows": "tsc && node dist/src/main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "~12.0.0",
    "lowdb": "~1.0.0",
    "typescript": "~3.8.3"
  },
  "devDependencies": {
    "@types/node": "^13.13.2",
    "@types/lowdb": "1.0.7"
  }
}
